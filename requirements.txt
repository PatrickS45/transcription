# Cœur du pipeline (CDC §7)
faster-whisper>=1.0        # transcription + VAD Silero intégré (moteur par défaut)
torch>=2.0                 # support CUDA — installer la build CUDA adaptée :
                           # pip install torch --index-url https://download.pytorch.org/whl/cu121
Pillow>=10.0               # calcul de largeur de texte rendu (étape 4)
gradio>=4.0                # interface locale (§11)

# Optionnel — étape 1bis (correction LLM)
# anthropic>=0.40          # backend API cloud (Claude)
# (le backend Ollama utilise l'API HTTP locale, aucune dépendance Python)

# Optionnel — moteur alternatif
# whisperx>=3.1

# Développement
pytest>=7.0
